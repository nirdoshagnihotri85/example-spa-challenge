using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace SPASample
{
    public enum ScreenSystem
    {
        PAYROLL = 1,
        HR = 2
    }

    public enum FieldStatus
    {
        Visible = 1,
        Hidden = 2,
        Disabled = 3
    }

}